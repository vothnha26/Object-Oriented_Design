@startuml
title Sequence Diagram for Task 1: Product Pricing (Decorator + Command)
autonumber

actor Client
participant "UI:Browser" as UI
participant "CartController" as CC
participant "UpdTopCmd:UpdateToppingsCommand" as UTC
participant ":CartHistoryManager" as CHM
participant ":CartService" as CS
participant "NewChain:PriceComponent" as PC
database DB

== Phase 1: Cập nhật Topping (Command Execution) ==
Client -> UI : Thay đổi số lượng topping & Submit
activate UI
UI -> CC : updateItemToppings(itemId, toppingsMap)
activate CC

CC -> UTC** : new UpdateToppingsCommand(cs, user, itemId, newMap, "Cập nhật topping")
CC -> CHM : executeCommand(cmd)
activate CHM

CHM -> UTC : execute()
activate UTC
UTC -> CS : getCurrentToppingQtys(itemId)
activate CS
CS --> UTC : oldToppingMap
deactivate CS

UTC -> CS : updateToppings(user, itemId, newMap)
activate CS

CS -> DB : deleteByCartItem(item)
CS -> DB : save(new SelectedToppings)
CS -> CS : recomputeLineTotal(item)
activate CS
CS -> PC** : Build Decorator Chain (Base -> Promo -> Topping -> Qty)
CS -> PC : calculate()
PC --> CS : newTotal
CS -> DB : Update CartItem lineTotal
deactivate CS

CS --> UTC : done
deactivate CS
UTC --> CHM : done
deactivate UTC
CHM --> CC : success
deactivate CHM

CC --> UI : Redirect to /cart
deactivate CC
UI --> Client : Hiển thị giá đã cập nhật
deactivate UI

== Phase 2: Hoàn tác (Undo) ==
Client -> UI : Click [Hoàn tác]
activate UI
UI -> CC : undo() [GET]
activate CC
CC -> CHM : undo()
activate CHM
CHM -> UTC : undo()
activate UTC
UTC -> CS : updateToppings(user, itemId, oldMap)
activate CS
CS -> DB : Restore previous Toppings state
CS -> CS : recomputeLineTotal(item)
CS --> UTC : done
deactivate CS
UTC --> CHM : done
deactivate UTC
CHM --> CC : success
deactivate CHM
CC --> UI : Redirect to /cart
deactivate CC
UI --> Client : Trạng thái & Giá quay về ban đầu
deactivate UI

@enduml
